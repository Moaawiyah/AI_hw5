# Core LLM stack
torch>=2.2
# Pin transformers to 4.x: AirLLM 2.11's Qwen2 attention layer-streaming is
# incompatible with the 5.x position_embeddings / 3D-hidden_states API.
transformers>=4.46,<5.0
accelerate>=0.30
sentencepiece
huggingface_hub>=0.23

# AirLLM (layer-streaming inference for out-of-RAM models)
airllm
# mlx and mlx-lm are no longer used: we bypass the macOS MLX gate and run
# AirLLMQWen2 (PyTorch/CPU) directly to support Qwen2.5 GQA architecture.
# mlx
# mlx-lm

# Quantization helpers (used as fallback / baseline loaders)
bitsandbytes; platform_machine != "arm64"
optimum

# Measurement & analysis
psutil
matplotlib
pandas
numpy

# Token counting for API economics
tiktoken

# API providers (economics comparison)
openai
anthropic

# .env loading
python-dotenv
